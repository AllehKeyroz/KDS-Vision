// This page is deprecated. The content has been moved to /prospects as a tab.
// Redirecting to the new location.
'use client';
import { redirect } from 'next/navigation';

export default function ProposalsRedirectPage() {
    redirect('/prospects');
}
